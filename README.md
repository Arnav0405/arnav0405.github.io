# my-portfolio

My personal portfolio site, built with Next.js.

## Dev

```bash
npm install
npm run dev

Open [http://localhost:3000](http://localhost:3000).
```

## Build

```bash
npm run build
``` 

Static site gets exported to out/, deployed to GitHub Pages via the Actions workflow on push to main.
